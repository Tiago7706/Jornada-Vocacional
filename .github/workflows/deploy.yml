# Deploy é feito automaticamente pela integração nativa Vercel + GitHub.
# Cada push para main dispara o deploy no Vercel sem necessidade deste workflow.
# Este arquivo existe apenas para documentação.
name: (info) Deploy via Vercel

on:
  workflow_dispatch: # só roda manualmente se necessário

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "O deploy é feito automaticamente pelo Vercel via integração GitHub."
